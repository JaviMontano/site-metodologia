node_modules/
.DS_Store
.playwright-mcp/
audit-*.png
*.png
!favicon.svg
!assets/**
!recursos/**

# Workspace — user interaction layer, not repo content
# Contains inputs, specs drafts, estandares, dated sessions, tasks bridge
workspace/
