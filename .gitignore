node_modules/
.DS_Store
.playwright-mcp/
audit-*.png
*.png
!favicon.svg
!assets/**
!recursos/**

# Generated page registry — regenerated by `npm run build:pages`
admin/data/page-registry.json

# Firebase Admin SDK service account keys — NEVER commit
serviceAccountKey.json
*.serviceAccount.json
*service-account*.json
secrets/

# Workspace — user interaction layer, not repo content
# Contains inputs, specs drafts, estandares, dated sessions, tasks bridge
workspace/
