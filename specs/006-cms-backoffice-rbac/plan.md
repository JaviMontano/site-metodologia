# Implementation Plan: CMS Backoffice RBAC

**Branch**: `006-cms-backoffice-rbac` | **Date**: 2026-03-23 | **Spec**: [spec.md](spec.md)
**Input**: Feature specification from `/specs/006-cms-backoffice-rbac/spec.md`

## Summary

Transform the binary-admin CMS into a 4-role RBAC system with user management, domain allowlisting, audit viewer, and content version restore. Fix the Firebase import error (BUG-003) by bundling with esbuild. Add Cloud Functions for privileged operations (role assignment, auto-provisioning). Extend the admin UI with new role-gated tabs.

## Technical Context

**Language/Version**: JavaScript ES2022 (browser + Node.js 20 for Cloud Functions)
**Primary Dependencies**: Firebase SDK v10 (modular), firebase-admin v12, firebase-functions v2, esbuild (bundler), idb v8
**Storage**: Firestore (document store) + Firebase Auth (identity + custom claims) + IndexedDB (client cache)
**Testing**: Vitest (unit), @firebase/rules-unit-testing (integration), Playwright (E2E)
**Target Platform**: Modern evergreen browsers (client) + Cloud Functions Node.js 20 (server)
**Project Type**: Web application (client-rendered SPA admin + serverless functions)
**Performance Goals**: Admin page load <2s (SC-001), Cloud Function cold start <500ms
**Constraints**: No custom server (Constitution I), Blaze plan required (R2), 1-10 CMS users
**Scale/Scope**: 63+ site pages, 4 role levels, 1-10 CMS users, 3 content collections + 4 new collections

## Constitution Check

*GATE: All 20 principles verified. No violations detected.*

| Principle | Status | Notes |
|-----------|--------|-------|
| I. Client-Rendered, Cloud-Backed | PASS | Admin is client-rendered, Cloud Functions are serverless |
| II. Accessibility-First | PASS | New tabs follow existing ARIA tablist pattern |
| III. SEO Integrity | PASS | Admin is noindex — exempt |
| IV. Component Consistency | PASS | New editors follow existing service module + editor pattern |
| V. Brand Separation | PASS | Admin UI uses MetodologIA branding only |
| VI. Content Authority | PASS | Audit log as version history; restore creates new write (reversibility) |
| VII. Secure by Default | PASS | RBAC rules, least privilege, irrevocable bootstrap, role audit trail |
| VIII. Offline Resilience | PASS | Admin exempt; shows connectivity warning |
| IX. TDD | PASS | Tests before code; hash-locked features |
| X. Design System | PASS | Admin uses existing Tailwind tokens |
| XI. Brand Voice | PASS | Admin is internal — voice rules exempt |
| XII. Code Sustainability | PASS | Business-readable naming, module pattern |
| XIII. Think First | PASS | This plan exists before code |
| XIV. Simple First | PASS | esbuild (1 cmd, no config), audit-as-versions (no extra collection) |
| XV. BDD Full-Spectrum | PASS | Scenarios cover security, UX, data, operational angles |
| XVI. Sequential-First | PASS | Phased delivery in dependency order |
| XVII. Continuous Learning | PASS | 3 RBAC insights already captured |
| XVIII. Indexable Repo | PASS | New dirs get READMEs |
| XIX. Bug Protocol | PASS | BUG-003 addressed as prerequisite |
| XX. Branch Parity | PASS | Feature → staging → main → production |

**G2 gate checklist**:
- [x] Data model defined (data-model.md)
- [x] API contracts defined (contracts/)
- [x] Security rules contract with RBAC (contracts/firestore-rules.md)
- [x] Role governance verified (INS-RBAC-001, INS-RBAC-002, INS-RBAC-003)
- [x] BDD scenarios ready for hash-locking (spec.md GWT)
- [x] Design tokens referenced (existing Tailwind + CSS custom properties)

## Project Structure

### Documentation (this feature)

```text
specs/006-cms-backoffice-rbac/
  spec.md              # Feature specification
  plan.md              # This file
  research.md          # Technical research (R1–R7)
  data-model.md        # Firestore data model
  quickstart.md        # Dev setup + test scenarios
  contracts/
    cloud-functions.md # Callable function contracts
    firestore-rules.md # Security rules contract
  checklists/          # Clarification checklists
  tasks.md             # Generated by /iikit-05-tasks
```

### Source Code (repository root)

```text
admin/
  index.html              # CMS shell — add new tabs, update script src
  js/
    admin-app.js          # MODIFY — role-based tab routing, idle timeout
    admin-bundle.js       # NEW — esbuild output (gitignored)
    program-editor.js     # MODIFY — enhance bilingual UX (FR-018)
    price-editor.js       # MODIFY — fix skeleton, add working save (FR-019)
    i18n-editor.js        # MODIFY — add bulk save (FR-020)
    user-manager.js       # NEW — user list, role assignment, invites (FR-016)
    audit-viewer.js       # NEW — audit log viewer with filters (FR-025)
    page-registry.js      # NEW — page listing with i18n coverage (FR-022), merges JSON + Firestore overrides
    profile-editor.js     # NEW — user profile and preferences (FR-015)
    idle-timer.js         # NEW — session timeout (FR-004)

js/cms/
  firebase-config.js      # UNCHANGED
  auth-service.js         # MODIFY — role-based checks instead of binary admin
  content-service.js      # UNCHANGED
  cache-manager.js        # UNCHANGED
  admin-api.js            # MODIFY — role-based audit entries, version restore
  migration-bridge.js     # UNCHANGED
  init.js                 # UNCHANGED

functions/                # NEW — Cloud Functions directory
  index.js                # setUserRole, inviteUser, removeUserAccess, onUserFirstLogin
  package.json            # firebase-functions, firebase-admin
  .eslintrc.js            # Standard Cloud Functions linting

firebase/
  firestore.rules         # MODIFY — RBAC security rules (v2)
  firestore.indexes.json  # MODIFY — add audit_log + users indexes

admin/
  data/
    page-registry.json    # NEW — generated by build:pages (gitignored)

scripts/
  seed-firestore.js       # MODIFY — seed config/access, bootstrap accounts
  set-admin-claim.js      # KEEP — backward compat, add deprecation notice
  set-user-role.js        # NEW — RBAC-aware role script
  migrate-claims.js       # NEW — one-time migration from admin:true to role:x
  build-page-registry.js  # NEW — scans **/*.html → page-registry.json

tests/
  unit/
    auth-service.test.js  # MODIFY — test role-based checks
    admin-api.test.js     # MODIFY — test role-based audit
    user-manager.test.js  # NEW — user management logic
    audit-viewer.test.js  # NEW — audit filtering + restore
    idle-timer.test.js    # NEW — timeout behavior
  integration/
    firestore-rules.test.js  # MODIFY — RBAC security rule tests
    cloud-functions.test.js  # NEW — callable function tests
  e2e/
    admin-flow.spec.js    # MODIFY — RBAC scenarios
    rbac-roles.spec.js    # NEW — role-based access E2E
```

**Structure Decision**: Extends existing flat directory layout. No new top-level directories except `functions/` (required by Firebase). New admin modules follow the existing `admin/js/*.js` editor pattern. [CODE]

## Architecture

```
┌─────────────────────────────────────────────┐
│                  Browser                     │
│                                              │
│  admin/index.html                            │
│    └─ admin-bundle.js (esbuild)              │
│         ├─ admin-app.js (router + auth)      │
│         ├─ program-editor.js                 │
│         ├─ price-editor.js                   │
│         ├─ i18n-editor.js                    │
│         ├─ user-manager.js      [super_admin]│
│         ├─ audit-viewer.js      [admin+]     │
│         ├─ page-registry.js     [editor+]    │
│         ├─ profile-editor.js    [all]        │
│         ├─ idle-timer.js                     │
│         └─ js/cms/* (7 modules)              │
│                                              │
│  ┌──────────┐  ┌───────────┐                 │
│  │ Firebase  │  │ IndexedDB │                 │
│  │ Auth SDK  │  │ Cache     │                 │
│  └─────┬─────┘  └─────┬─────┘               │
└────────┼───────────────┼──────────────────────┘
         │               │
         ▼               │
┌─────────────────────────────────────────────┐
│            Firebase Backend                   │
│                                              │
│  ┌──────────────┐  ┌──────────────────────┐  │
│  │ Firebase Auth │  │ Cloud Functions v2   │  │
│  │ (Google OAuth)│  │                      │  │
│  │ Custom Claims │  │  setUserRole()       │  │
│  │  { role: ... }│  │  inviteUser()        │  │
│  │               │  │  removeUserAccess()  │  │
│  └───────────────┘  │  onUserFirstLogin()  │  │
│                     └──────────────────────┘  │
│  ┌──────────────────────────────────────────┐│
│  │ Firestore                                ││
│  │                                          ││
│  │  programs/{id}       (public read)       ││
│  │  pricing/{cat}       (public read)       ││
│  │  translations/{lang} (public read)       ││
│  │  users/{uid}         (role-gated)        ││
│  │  config/access       (access rules)      ││
│  │  config/invites/*    (pending invites)   ││
│  │  audit_log/{id}      (immutable trail)   ││
│  │  config/settings     (system config)     ││
│  └──────────────────────────────────────────┘│
└──────────────────────────────────────────────┘
```

## Implementation Phases

### Phase 0: Foundation (BUG-003 + Build Pipeline)
**Priority**: P0 — blocks all other work

1. Add esbuild as dev dependency + `build:admin` and `watch:admin` npm scripts
2. Bundle `admin/js/admin-app.js` → `admin/js/admin-bundle.js`
3. Update `admin/index.html` script src to bundle
4. Add `admin/js/admin-bundle.js` to `.gitignore`
5. Verify admin page loads with zero console errors

**Validates**: FR-001, SC-001, BUG-003

### Phase 1: Auth & RBAC Core
**Priority**: P1 — security foundation

1. Initialize `functions/` directory with Cloud Functions project
2. Implement `onUserFirstLogin` trigger (auto-provision logic)
3. Implement `setUserRole` callable
4. Update `auth-service.js`: replace `isAdmin()` with `getRole()`, `hasPermission(level)`
5. Update `firestore.rules` to RBAC v2 (with legacy fallback)
6. Update `seed-firestore.js` to seed `config/access` + bootstrap accounts
7. Update `admin-app.js` for role-based tab rendering

**Validates**: FR-003, FR-005, FR-006, FR-007, FR-008, FR-009, FR-010, FR-013, SC-002, SC-008

### Phase 2: User Management + Access Control
**Priority**: P1 — completes security layer

1. Create `user-manager.js` (user list, role assignment UI)
2. Implement `inviteUser` and `removeUserAccess` Cloud Functions
3. Add invite management UI to user manager
4. Implement domain allowlist management UI
5. Add idle timeout (`idle-timer.js`)
6. Implement `profile-editor.js`

**Validates**: FR-004, FR-011, FR-012, FR-014, FR-015, FR-016, FR-017, SC-003

### Phase 3: Content Editors Enhancement
**Priority**: P1 — core CMS function

1. Enhance `program-editor.js` — bilingual side-by-side, stale indicators (FR-018)
2. Fix `price-editor.js` — working inputs, save, B2C/B2B/premium (FR-019)
3. Add bulk save to `i18n-editor.js` (FR-020)
4. Add unsaved changes indicator + navigation guard to all editors (FR-021)

**Validates**: FR-018, FR-019, FR-020, FR-021, SC-004

### Phase 4: Page Management + Audit
**Priority**: P2 — extends CMS scope

1. Create `page-registry.js` — scan site, show pages with i18n coverage (FR-022, FR-023, FR-024)
2. Create `audit-viewer.js` — chronological log with filters (FR-025, FR-026)
3. Implement version restore from audit entries (FR-027)
4. Add login/logout event logging (FR-028)

**Validates**: FR-022, FR-023, FR-024, FR-025, FR-026, FR-027, FR-028, SC-005, SC-006, SC-007

## Dependencies & Critical Path

```
Phase 0 (BUG-003) ──→ Phase 1 (RBAC Core) ──→ Phase 2 (User Mgmt)
                                              ──→ Phase 3 (Editors) [parallel-eligible with Phase 2]
                      Phase 1 ──→ Phase 4 (Pages + Audit)
```

Phase 2 and Phase 3 are parallel-eligible per XVI (different files, no data dependency, no logical dependency). Phase 4 depends on Phase 1 (needs RBAC for audit visibility rules).

## Key Design Decisions

| Decision | Rationale | Constitutional Anchor |
|----------|-----------|----------------------|
| esbuild, not Vite | Zero config, 1 command, already in deps | XIV (Simple First) |
| Dual storage (claims + Firestore) | Rules need fast claim check; UI needs queryable Firestore | VII, R3 |
| Audit log as version history | No extra collection; reconstruct from entries | XIV, VI |
| All-content editor access | Simpler than per-doc assignment at 1-10 users | XIV, INS-RBAC-002 |
| Bootstrap as security invariant | Cannot lock out all super_admins | VII, INS-RBAC-003 |
| Client-side idle timeout | No server-side revocation needed at this scale | XIV, R5 |
| Legacy claim fallback | Smooth migration from `admin: true` to `role: x` | VI, VIII |

## Clarifications

### Session 2026-03-23

- Q: How should existing users with old `admin: true` claim migrate to new `role: x` claim? -> A: Both layers — (1) one-time migration script `scripts/migrate-claims.js` converts known users, (2) client-side detection in `admin-app.js`: if `token.admin === true && !token.role`, call `migrateMyRole` Cloud Function as self-healing fallback. [Phase 1, R3, data-model.md custom claims, contracts/cloud-functions.md]
- Q: How are `last_login` and `total_sessions` updated on subsequent logins? -> A: Client-side in `admin-app.js` onAuthStateChanged handler. Writes `last_login: serverTimestamp()`, `total_sessions: increment(1)` to own `users/{uid}` doc + audit_log entry (action: login). Logout via signOut handler + `navigator.sendBeacon()` on beforeunload (best-effort). Security rules allow self-update of `last_login`, `total_sessions` only. Socratic debate confirmed: informational fields don't warrant server-side tracking (INS-OPS-001). [Phase 2, FR-017, FR-028, data-model.md users, contracts/firestore-rules.md]
- Q: What is the source of truth for bootstrap accounts, and how is divergence prevented? -> A: Deploy-time environment config is sole authority. Bootstrap accounts defined in `functions.config().bootstrap.accounts` (Firebase environment config) or `.env` file — never hardcoded in source code (Constitution XXI). `onUserFirstLogin` reads env config, lazy-syncs `config/access.bootstrap_accounts` for UI display. `setUserRole` reads env config to block demotion. Firestore field is read-only mirror, not writable from client. Socratic debate ratified + amended by XXI: security invariants live in deploy-time config, not source code or runtime stores (INS-RBAC-004 v2). [Phase 1, FR-013, data-model.md config/access, contracts/cloud-functions.md onUserFirstLogin + setUserRole]
- Q: What hash function for `config/invites/{email_hash}` doc IDs? -> A: No hash — use sanitized email directly. Lowercase email with `.` and `@` replaced by `_` (e.g., `partner_aliado_com`). Zero crypto, directly reversible, Firestore-safe. [Phase 2, FR-012, data-model.md invites, contracts/cloud-functions.md inviteUser]
- Q: Should there be an esbuild watch mode for development? -> A: Yes. Add `"watch:admin"` npm script (`esbuild ... --watch`). Same binary, zero config, eliminates manual rebuild friction. [Phase 0, R1, quickstart.md]
- Q: How is the 90-day audit log TTL enforced? -> A: Firebase Scheduled Function (`onSchedule`) runs daily, queries `audit_log` where `ttl < now()`, batch deletes expired entries. Added as `cleanupExpiredAudit` in functions/index.js. [Phase 4, FR-025, data-model.md audit_log, contracts/cloud-functions.md]
- Q: How does the page registry detect and list site pages? -> A: Hybrid — build-time scan + Firestore overrides. (1) `scripts/build-page-registry.js` scans `**/*.html`, extracts title/meta/data-i18n counts/level, outputs `admin/data/page-registry.json`. Run as part of `build:admin`. (2) `page_overrides/{path_hash}` Firestore collection stores admin-edited metadata (title, description per lang). (3) `page-registry.js` merges JSON base + Firestore overrides. Socratic debate: filesystem facts discovered at build-time, CMS customizations in Firestore (INS-DATA-001). [Phase 4, FR-022, FR-023, FR-024, data-model.md page_overrides]

## Complexity Tracking

No violations of XIV (Simple First) detected. All solutions use the simplest viable approach for 1-10 CMS users at current scale.
